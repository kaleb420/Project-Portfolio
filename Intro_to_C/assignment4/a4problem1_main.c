#include <stdio.h>
int main(void){
	const char* str;
	char* destination;
	char* source;
	int n, character;
	strLen(str);
	strCpy(destination, source);
	strNCpy(destination, source, n);
	strCat(destination, source);
	strChr(str, character);
}
