import java.util.Iterator;
import java.util.List;

//public class EnumerateLazyList<T> implements ILazyList<EnumerateLazyList.EnumerateItem<T>>, Iterator<EnumerateLazyList.EnumerateItem<T>>, Iterable<EnumerateLazyList.EnumerateItem<T>>{

    /*private int index;
    private T item;
    private List<T> l;

    /**
     * develops support for tuples in java, stores an index and the item at that index
     * @return
     */
    /*public static <T> EnumerateItem(){

    }

    int getIndex(){
        return index;
    }

    T getItem(){
        return item;
    }

    /**
     * Returns {@code true} if the iteration has more elements.
     * (In other words, returns {@code true} if {@link #next} would
     * return an element rather than throwing an exception.)
     *
     * @return {@code true} if the iteration has more elements
     */
    /*@Override
    public boolean hasNext() {
        return false;
    }

    /**
     * computes the next element in the list
     *
     * @return next element in the list
     */
    /*@Override
    public EnumerateLazyList.EnumerateItem<T> next() {
        return
    }

    /**
     * Returns an iterator over elements of type {@code T}.
     *
     * @return an Iterator.
     */
    /*@Override
    public Iterator<EnumerateLazyList.EnumerateItem<T>> iterator() {
        return this;
    }

    /**
     * overrides the to string function to format a string according to instructions
     * @return index and item at that index as a string
     */
    /*@Override
    public String toString(){
        return "(" + String.valueOf(getIndex()) + "," + getItem() + ")";
    }

    /**
     * constructor to initialize the list to enumerate as an instance variable, and the current item accessed
     * @param ls list given
     */
    /*private EnumerateLazyList(List<T> ls){

    }

    /**
     * public constructor to initialize the private constructor
     * @param ls list given
     * @return new object created
     * @param <T> type of data being used
     */
    /*static <T> EnumerateLazyList<T> enumerator(List<T> ls){

    }
}
*/
