public class Problem2 {
    /**
     * Combines two digits into a two digit number
     * @param a first digit
     * @param b second digit
     * @return the combined digits
     */
    static int combineDigits(int a, int b){
        return (a*10)+b;
    }
}
