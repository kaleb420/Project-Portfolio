#include <blib.h>
#include <system.h>

#define PROMPT "shell$ "

/* --------------------------------------------------- *
 * 'shell' runs an infinite loop  that prints a prompt *
 * to the user, reads in a command, parses the command *
 * and calls the corresponding function.               *
 * --------------------------------------------------- */
u8 shell([[gnu::unused]] char* arg) {
  
  return 0;
}
